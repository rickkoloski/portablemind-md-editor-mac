# Handoff (stub — D2)

Purpose: the Submit / Handoff protocol from `docs/vision.md` Principle 1 (Level 2 agent-aware). Bidirectional change detection plus an explicit **Submit** verb that signals "your turn" to an agent.

Status: Stub only. No Swift source at D2. The module directory exists so a later feature deliverable plugs in at a stable boundary.

Expected D6+ deliverable per `docs/roadmap_ref.md` — likely alongside the PortableMind integration, since in connected mode Submit becomes a real status transition on the associated task. Standalone-mode realization (sidecar marker, git commit, trailing comment) may land earlier.
