# Keyboard (stub — D2)

Purpose: keyboard shortcut map — command → chord table. Word/Docs-first by default (Cmd+B bold, Cmd+I italic, Cmd+K link, Cmd+Shift+7/8 lists, Cmd+Opt+1..6 headings) per `docs/competitive-analysis.md` scoping question Q6.

Status: Stub only. No Swift source at D2.

Expected deliverable: **D4** per `docs/roadmap_ref.md` — source-mutation primitives arrive together with their keyboard bindings. The toolbar in D5 is a second way to invoke the same primitives.
